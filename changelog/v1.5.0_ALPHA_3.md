# 1.5.0_ALPHA_3 — 2026-05-06

Comprehensive security and stability release — 50 issues fixed across permissions, async networking, concurrency, and block-entity contracts. Server administrators should review permission configurations as several bypasses are now correctly enforced.

## Security

| Tag | Change |
|---:|---|
| ![bugfix] | <details><summary>Permission bitmask bypass in deposit, withdraw, and admin checks</summary><br>`BankPermission.ordinal()` returns 0/1/2 but bitmask values are 1/2/4. All three call sites now use `getValue()` so DEPOSIT/WITHDRAW/MANAGE checks enforce the correct bit.</details> |
| ![bugfix] | `/banksystem deop` (self and user variants) granted admin instead of revoking — now correctly passes `false` |
| ![bugfix] | `DeleteBankAccountAsync` permission gate used OR instead of AND, blocking deletion when the player was non-admin OR lacked MANAGE — now admin OR MANAGE allows deletion |
| ![bugfix] | <details><summary>BankUploadBlockEntity ownership bypass</summary><br>`handlePacket` applied `dropIfNotBankable` and `bankAccountNumber` settings before the ownership check, letting any sender redirect items to a chosen account. Settings now applied only inside the ownership guard.</details> |
| ![bugfix] | <details><summary>UpdateBankAccountRequest accepted arbitrary permission values</summary><br>Clients with MANAGE could set any permission integer. Values are now masked to the valid bitmask, and the bank owner's permissions can no longer be modified by other users.</details> |
| ![bugfix] | Client-callable `SetAccountNameAsync` and `SetAccountIconAsync` had no permission check — now requires MANAGE permission |
| ![bugfix] | <details><summary>Integer overflow in WithdrawMoneyPacket allowed item duplication</summary><br>`requestedAmount * itemValue` could overflow `long`, letting crafted packets withdraw a tiny amount but receive huge stacks. Added pre-multiplication overflow guard.</details> |
| ![bugfix] | `BankTerminalBlockDataRequest` cast block entity without `instanceof` — malicious client with wrong BlockPos could crash the server |
| ![bugfix] | `WithdrawItemsFromBankRequest` returned requested items on every error path, signaling success when none occurred — now returns empty map on rejection |
| ![bugfix] | `DepositItemsInBankRequest` accepted negative amounts — now clamped to zero |
| ![hardening] | 8-block range check added to all four block-entity packet handlers, preventing interaction with entities anywhere in the loaded world |

## Performance

| Tag | Change |
|---:|---|
| ![bugfix] | <details><summary>Netty buffer leak and oversized wire payloads in async operations</summary><br>`AsyncFunctionInputData.of` and `AsyncFunctionOutputData.of` allocated `Unpooled.buffer()` and never released them — every async call leaked the buffer. Both now wrapped in try/finally with `release()`.<br><br>Additionally, `buf.array()` returned the full 256-byte backing array including unused capacity. Replaced with `ByteBufUtil.getBytes(buf)` to send only the readable region.</details> |

## API

| Tag | Change |
|---:|---|
| ![feature] | <details><summary>Type-safe `hasPermission(UUID, BankPermission)` overload</summary><br>Added to `ISyncServerBankAccount`, `IAsyncBankAccount`, and `BankAccountData`. All 13 production call sites updated to pass the enum directly instead of `getValue()` — eliminates the ordinal/value confusion that caused the original permission bypass. The int-based overload is kept for compatibility.</details> |
| ![hardening] | All banking API methods now reject both `null` and `INVALID_ID` ItemIDs — 13 entry points in `ServerBank`, `ServerBankAccount`, and `ServerBankManager` |
| ![bugfix] | Four async lookups (`getPersonalBankAccountAsync`, `getPersonalBankAsync` by UUID and name) hung forever on missing accounts — now complete with `null` |
| ![bugfix] | `GetBankAccountNrByNameAsync` leaked an `IServerBankAccount` through the codec instead of returning the account number as documented |

## CLI

| Tag | Change |
|---:|---|
| ![bugfix] | `/money remove` compared raw cents (`getBalance()`) against real units, allowing incorrect removals — now uses `getRealBalance()` |

## Internal

| Tag | Change |
|---:|---|
| ![bugfix] | <details><summary>4 async forwarding fixes</summary><br><ul><li>`isSlaveServerTrustedAsync` sent wrong FunctionType (`IsBanksystemAdminAsync`), breaking slave-trust validation in multi-server mode</li><li>`GetMinimalDataAsync` codec registered as `BankManagerData.STREAM_CODEC` but handler returns `BankData` — serialization failure</li><li>`bank_create_async` used `handleResponse` which casts to `Boolean`, but the handler returns `Integer` — ClassCastException</li><li>`removeAllBanksAsync` sent `RemoveEmptyBanksAsync` instead of `RemoveAllBanksAsync`</li></ul></details> |
| ![bugfix] | <details><summary>9 banking logic method delegation fixes</summary><br><ul><li>`withdrawLockedAsync` called `withdraw()` instead of `withdrawLocked()` — wrong balance pool</li><li>`hasUserAsync` called `hasAnyUser()` instead of `hasUser(uuid)`</li><li>`hasBankAsync` called `hasAnyBank()` instead of `hasBank(itemID)`</li><li>`toStringNoOwnerAsync` returned `toJsonString()` instead of `toStringNoOwner()`</li><li>`getNormalizedTotalBalanceAsync` and `getFormattedTotalBalanceAsync` used only locked balance instead of total</li><li>`createBankAccountAsync` missing `return` on failure path — fell through to setup on null account</li><li>`getOrCreatePersonalBankAccountAsync` checked the CompletableFuture reference for null (never null) instead of the value</li><li>`getNotRemovableItems` returned blacklist items instead of not-removable items</li><li>`getOrCreatePersonalBankAccount` passed `INVALID_ACCOUNT_NUMBER` as starting balance instead of literal `0`</li></ul></details> |
| ![bugfix] | <details><summary>3 arithmetic and precision fixes</summary><br><ul><li>`convertToRawAmountStatic("10.5")` produced 1005 instead of 1050 — fractional part not padded to scale-factor width</li><li>`convertToRealAmountStatic` used `float` cast losing precision past ~7 significant digits — replaced with `double`</li><li>`getNormalizedAmountStatic` could divide by zero, throw StringIndexOutOfBounds, or produce NaN — added guards for edge cases</li></ul></details> |
| ![bugfix] | <details><summary>4 concurrency fixes</summary><br><ul><li>`ServerBankManager.removeUser` mutated map while iterating — collect-then-delete</li><li>`ServerBankAccount.unsubscribeBankChanges` removed listeners during iteration — direct `remove()` call</li><li>`BankAccountChangeStream.nextPacket` written by callback thread, read by tick thread without sync — marked `volatile`</li><li>`BankSystemMod.init()` had unsynchronized lazy init — now `synchronized` with `volatile` backend field</li></ul></details> |
| ![bugfix] | <details><summary>8 block-entity contract fixes</summary><br><ul><li>`TerminalInventory` get/remove methods returned `null` instead of `ItemStack.EMPTY` — caused NPEs in shift-clicks and hopper interaction</li><li>`TerminalInventory.load()` always returned `false` even on success</li><li>`TerminalInventory.getItemCount` checked the wrong variable for null</li><li>`TerminalInventory.getFreeSpace` hardcoded 64 instead of using the queried amount</li><li>`BankTerminalBlockEntity` used client-side `ItemID` registration from server-side code</li><li>`BankTerminalBlock.onRemove` always dropped items even on same-block state changes</li><li>`BankDownloadBlock` opened menu off the server main thread inside a `CompletableFuture` callback</li><li>`MoneyStockpileBlockEntity.dropItems` produced invalid stacks exceeding `maxStackSize` — now splits into multiple drops</li></ul></details> |
| ![bugfix] | `BankSystemDataHandler.save_bank` overwrote valid save data when `bankManager` was null — added early-return guard |
| ![bugfix] | `setAccountName` had redundant empty-string rewrite; `setAccountIcon` used `!=` instead of `Objects.equals` for change detection |
| ![bugfix] | <details><summary>2 test assertion fixes</summary><br><ul><li>`ArithmeticTests.testConvertToRawAmountSingleDecimal` updated from buggy assertion (1005) to correct result (1050)</li><li>`AsyncForwardingTests.testGetMinimalDataAsyncCodecIsBankData` replaced identity comparison with round-trip encode/decode — the `nullable()` wrapper breaks identity</li></ul></details> |
| ![feature] | <details><summary>49 new regression tests across 8 suites</summary><br><ul><li>19 arithmetic tests: null/empty/garbage input, comma decimals, excess fraction digits, sub-unit values, custom scales, `Long.MAX_VALUE` precision, negative input, exponent boundaries, formatting, round-trip preservation</li><li>7 multi-server security tests: overflow guard math, permission bitmask validation, withdraw/deposit rejection paths, empty-bank-creation prevention</li><li>4+4 async forwarding tests: encode length validation, input/output round-trips, exhaustive FunctionType→codec entry checks for AsyncBank/AsyncBankAccount/AsyncBankManager, primitive output codec round-trips</li><li>2+3 networking/command tests: distance constant, distance math, op-command gate, allowItem/disallowItem admin gates</li><li>6 serialization tests: codec round-trip for BankData, BankAccountData, BankManagerData, BankUserData, UserData, ItemInfoData</li><li>3 lifecycle tests: volatile nextPacket check, synchronized init + volatile backend check, buffer leak smoke test</li><li>1 permission test: type-safe `hasPermission` enum overload matches int variant</li></ul></details> |

<!-- Badge definitions — edit colors here once, applied everywhere. Style: flat-square. -->
[feature]:     https://img.shields.io/badge/feature-10b981?style=flat-square
[improvement]: https://img.shields.io/badge/improvement-0ea5e9?style=flat-square
[bugfix]:      https://img.shields.io/badge/bugfix-e11d48?style=flat-square
[breaking]:    https://img.shields.io/badge/breaking-1e293b?style=flat-square
[removal]:     https://img.shields.io/badge/removal-f97316?style=flat-square
[deprecation]: https://img.shields.io/badge/deprecation-eab308?style=flat-square
[hardening]:   https://img.shields.io/badge/hardening-8b5cf6?style=flat-square
