# 2.0.1 — 2026-05-21

## Security

| Tag | Change |
|---:|---|
| ![bugfix] | <details><summary>Async forwarding rejected valid client requests on the master server</summary><br>When a client played directly on the master server, `handleOnServer` passed an empty slave ID. The untrusted-slave security check fired first and rejected the request because the empty ID wasn't in the trusted list — the client whitelist was never consulted. Extracted security logic into a shared `isRequestAllowed` method that skips the slave trust check when no slave is involved.</details> |

## API

| Tag | Change |
|---:|---|
| ![feature] | <details><summary>SQLite-based balance history for graph visualization</summary><br>Periodic snapshots of all bank account balances are now stored in a per-world SQLite database (`data/BankSystem/Database/banksystem.db`). The `BalanceHistoryManager` provides filtered query and deletion APIs by date range, account number, and item type — ready for future graph UI integration. Snapshot interval is configurable via `BALANCE_SNAPSHOT_INTERVAL_MINUTES` server setting (default: 1 min). Master server only.</details> |
| ![feature] | <details><summary>Extensible per-user custom data on the User object</summary><br>Added `CompoundTag customData` field to `User` for storing arbitrary per-user settings. Persisted in NBT on the master server, synced to clients and slaves via StreamCodec. Accessible through new ARRS requests `GetUserCustomDataRequest` and `UpdateUserCustomDataRequest`.</details> |
| ![feature] | <details><summary>Item price provider API for total wealth tracking</summary><br>New `ItemPriceProvider` functional interface and `setPriceCurrencyItem()` on `BankSystemAPI` allow external mods (e.g. StockMarket) to supply item market prices. When registered, each balance snapshot calculates total portfolio wealth per account and stores it as a synthetic "Total Wealth" line (gold, pinned on top, toggleable). See `documentation/developer-guide/ItemPriceProviderAPI.md`.</details> |

## UI / UX

| Tag | Change |
|---:|---|
| ![feature] | <details><summary>Balance history chart screen with interactive line graph</summary><br>New "History" button in the Bank Terminal opens a full-screen line chart showing balance over time for all items in the account. Features include:<ul><li>Scrollable and zoomable on both axes (mouse drag to pan, scroll wheel to zoom, Shift/Ctrl for axis lock)</li><li>Multiple item series overlapped with distinct colors</li><li>Item filter sidebar with item icons and toggle buttons to show/hide individual series</li><li>Search field with tag-based matching (e.g. "wood" matches all wood-related items)</li><li>Hover highlight: hovering a filter entry brightens and thickens its chart line, dims others, and draws it on top</li><li>Filter state persists across sessions via user custom data on the master server</li><li>Account name shown in title bar</li></ul></details> |
| ![improvement] | Bank Terminal balance display uses compact formatting for large amounts (e.g. 1.500M, 12.345k) to prevent text overlapping the input field |
| ![improvement] | Chart line colors derived from item texture pixels with saturation boost, cached per session |
| ![feature] | `BALANCE_SNAPSHOT_MAX_RECORDS_PER_ITEM` setting — limits stored snapshots per item per account (default 1440). Set to 0 for unlimited with startup warning about database growth |
| ![bugfix] | Balance history chart no longer shows future time when zooming out or using auto-center (Space key) |
| ![bugfix] | Balance history chart now correctly renders zero-balance items with proper Y-axis padding |
| ![bugfix] | Fixed chart scissor rect alignment — lines no longer bleed outside the frame border |
| ![bugfix] | Fixed crash in BankUploadScreen when checkbox callback fired during construction |
| ![bugfix] | Fixed multiple NPE crashes in BankAccountManagementScreen when UI elements were accessed before async initialization completed |
| ![improvement] | Item filter buttons in the balance history sidebar now use a brightness-boosted hover color matching the chart line highlight |
| ![improvement] | <details><summary>Unified three display blocks into a single configurable block</summary><br>Merged the Bank Display, Balance Display, and Balance History Display blocks into one configurable block. Right-click to select display type (Balance Overview or Balance History) and bank account, then right-click again to open the synced interaction screen. All state persists in block entity NBT; configured displays update live.</details> |
| ![improvement] | Display block balance overview uses async API for multi-server slave compatibility |
| ![bugfix] | Balance overview no longer flashes "loading..." on every periodic update |
| ![bugfix] | Balance history chart no longer auto-scrolls past the last data point |
| ![improvement] | Newly placed display blocks automatically adopt the configuration of adjacent configured displays, expanding the multi-block display |
| ![improvement] | Chart line colors across all displays now derived from item texture pixels with saturation boost and brightness floor, replacing the fixed color palette |
| ![improvement] | BalanceHistoryChart supports state serialization for display block sync and adds `scrollToPresent()` for live auto-scrolling |
| ![improvement] | Hovering a legend icon in the display block's balance history interaction screen highlights the corresponding chart line |
| ![improvement] | Bank Display block uses ModUtilities display panel textures with distinct front face |
| ![feature] | Crafting recipe for Bank Display block (display + glass panes + iron nuggets) |
| ![bugfix] | Download block now properly handles clearing the bank account — setting account to zero disconnects the block and syncs state to client instead of silently failing |
| ![bugfix] | Balance overview display now shows newly added items — client-side GUI rebuilds when the item count changes |
| ![improvement] | Refreshed all block and item textures with optimized pixel art |

## Build & Dependencies

| Tag | Change |
|---:|---|
| ![feature] | Added `sqlite-jdbc 3.46.0.0` dependency with shadow-bundling on all platforms (Fabric, NeoForge, Quilt) |
| ![improvement] | Migrated GUI imports to MC_ModUtilities v2.0.1 `gui.client` package — `GuiScreen`, `GuiContainerScreen`, and `ContainerView` moved from `gui` / `gui.elements` to `gui.client`; replaced removed `getMinecraft()` and `getRoot().getScreen()` accessors |

## Internal

| Tag | Change |
|---:|---|
| ![bugfix] | Server cleanup nulls all INSTANCES fields on stop, preventing stale state across singleplayer world reloads |
| ![bugfix] | Fixed dedicated server crash from client class loading in display block |
| ![improvement] | Extracted shared `ItemColorUtil` — texture-based item color extraction with saturation boost and brightness clamping, cached per ItemID. Replaced ~100 lines of duplicate color logic in `BalanceHistoryScreen` |
| ![improvement] | Deduplicated async forwarding security checks — extracted `isRequestAllowed` into `AsyncForwardingRequest` base class, replacing identical 12-line blocks in `AsyncBank`, `AsyncBankAccount`, and `AsyncBankManager` |
| ![bugfix] | Updated zh_cn.json translations — added 66 missing keys, removed 5 stale keys, and fixed variable mismatch in `ask_remove_message` |
| ![feature] | Added 14 in-game database tests (`/banksystem test database`) covering record round-trips, filter queries, batch inserts, ordering, limits, deletion, and timestamp preservation |
| ![improvement] | Added dedicated git-changelog agent definition for standardized commit message formatting and pre-commit workflow |
| ![improvement] | Moved mod data storage from `Finance/BankSystem` to `data/BankSystem` with automatic migration and backup of old data |

## Documentation

| Tag | Change |
|---:|---|
| ![feature] | Added comprehensive async function forwarding architecture documentation with flow diagrams, security model, and step-by-step guide for adding new forwarded methods |
| ![improvement] | Restructured documentation into user-guide and developer-guide sections with dedicated pages for usage, bank accounts, administration, multi-server setup, commands, and API reference |
| ![improvement] | Restructured README for players — reordered sections so Usage comes before Crafting Recipes, added Installation guide, expanded Features list, and fixed broken HTML |
| ![feature] | Added Bank Display and Money Stockpile sections to Usage documentation with configuration screen and in-game screenshots |
| ![improvement] | Converted all documentation pages to top-image layout for better visibility of GIFs and screenshots |
| ![bugfix] | Added missing `banksystem_display_block` translation key to de_de, de_ch, and zh_cn language files |

<!-- Badge definitions — edit colors here once, applied everywhere. Style: flat-square. -->
[feature]:     https://img.shields.io/badge/feature-10b981?style=flat-square
[improvement]: https://img.shields.io/badge/improvement-0ea5e9?style=flat-square
[bugfix]:      https://img.shields.io/badge/bugfix-e11d48?style=flat-square
[breaking]:    https://img.shields.io/badge/breaking-1e293b?style=flat-square
[removal]:     https://img.shields.io/badge/removal-f97316?style=flat-square
[deprecation]: https://img.shields.io/badge/deprecation-eab308?style=flat-square
[hardening]:   https://img.shields.io/badge/hardening-8b5cf6?style=flat-square
